#include <stdio.h>
int arrange(int x[]);
int main(){
    int a[] = {7, 2, 8, 1, 0, 4, 3};
    //if only one number remained
    // quit
    // sort the left halt
    // sort the right half



}
int arrange(int x[]{
    
}